{
  "name": "@beautymade/face-engine",
  "version": "0.1.0",
  "private": true,
  "description": "Shared face topology, mesh subdivision and beauty deformation engine for BeautyMade.",
  "main": "src/index.ts",
  "types": "src/index.ts",
  "sideEffects": false,
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "typescript": "~6.0.3",
    "vitest": "^5.0.1"
  }
}
